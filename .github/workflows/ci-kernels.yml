name: Build and run kernels

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-and-run-kernels:
    runs-on: ubuntu-latest
    # FIXME use an explicit hash to pin the image version
    container: ghcr.io/nazavode/snitch-toolchain:2.0
    steps:
      - uses: actions/checkout@v3
      # - run: make install
      #   working-directory: snitch/mlir-runtime
      - run: make allrun
        working-directory: kernels/axpy/64xf32
