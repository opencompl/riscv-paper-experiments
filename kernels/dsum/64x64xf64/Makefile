.DEFAULT_GOAL := all

include ../../../snitch/Makefile.rules

TESTS =
TESTS += baseline.x
TESTS += snrt.x
TESTS += snitch_stream.x
TESTS += linalg.x
TESTS += linalg_xdsl.x

include ../../Makefile.kernels
