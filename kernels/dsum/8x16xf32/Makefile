.DEFAULT_GOAL := all

include ../../../snitch/Makefile.rules

TESTS =
TESTS += baseline.x
TESTS += ssr1d.x
TESTS += snrt.x
TESTS += ssr2d.x
TESTS += linalg.x
TESTS += scf.x
TESTS += linalg_2.x

include ../../Makefile.kernels
