.DEFAULT_GOAL := all

include ../../../snitch/Makefile.rules

TESTS =
TESTS += baseline.x
TESTS += ssr1d.x
TESTS += snrt.x
TESTS += ssr2d.x
TESTS += snitch_stream.x
TESTS += linalg.x
TESTS += linalg_xdsl.x
TESTS += scf.x
TESTS += scf_xdsl.x

include ../../Makefile.kernels
