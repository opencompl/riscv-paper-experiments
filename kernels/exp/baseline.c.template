#include "data.h"

#include <snrt.h>

#include <math.h>
#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

void exp_kernel(const DTYPE* x, DTYPE* z, double *workspace, size_t workspace_size) {
    if (snrt_cluster_core_idx() != 0) return;
    for (uint32_t i = 0; i < N; ++i) {
        z[i] = EXPF(x[i]);
    }
}

#ifdef __cplusplus
}
#endif
