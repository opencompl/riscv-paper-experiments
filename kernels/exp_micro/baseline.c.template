#include "data.h"

#include <snrt.h>

#include <math.h>
#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

void exp_kernel(const DTYPE* x, DTYPE* z) {
    for (uint32_t i = 0; i < N; ++i) {
        z[i] = EXPF(x[i]);
    }
}

#ifdef __cplusplus
}
#endif
