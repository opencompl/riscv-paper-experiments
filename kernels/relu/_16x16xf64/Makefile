.DEFAULT_GOAL := all

include ../../../snitch/Makefile.rules

TESTS =
TESTS += baseline.x
TESTS += ssr.x
TESTS += ssr_frep.x
TESTS += snrt.x
TESTS += linalg.x
TESTS += snitch_stream.x

include ../../Makefile.kernels
